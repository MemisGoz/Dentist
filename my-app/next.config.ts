import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "aceternity.com", "images.pexel.com", "assets.aceternity.com", "res.cloudinary.com"],
  },
};

export default nextConfig;
